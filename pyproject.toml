[project]
name = "maven-ai"
version = "0.1.0"
description = "Learning project for AI systems coursework."
requires-python = ">=3.11,<3.12"
dependencies = [
  "gradio",
  "langchain>=1.2.10",
  "langchain-openai>=1.1.9",
  "langgraph>=1.0.8",
  "python-dotenv",
]

[tool.uv]
package = false
